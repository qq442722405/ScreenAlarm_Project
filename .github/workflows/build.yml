name: Build EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Generate spec file (English name)
        run: |
          pyi-makespec --onefile --windowed --name "MonitorApp" --icon favicon.ico --add-data "警报声.mp3;." --add-data "favicon.ico;." main.py

      - name: Modify spec to include OpenCV/NumPy (avoid Chinese in PowerShell)
        shell: powershell
        run: |
          $spec = Get-Content 'MonitorApp.spec' -Raw
          $insert = @"
          from PyInstaller.utils.hooks import collect_all
          cv2_datas, cv2_binaries, cv2_hiddenimports = collect_all('cv2')
          a.datas.extend(cv2_datas)
          a.binaries.extend(cv2_binaries)
          a.hiddenimports.extend(cv2_hiddenimports)
          numpy_datas, numpy_binaries, numpy_hiddenimports = collect_all('numpy')
          a.datas.extend(numpy_datas)
          a.binaries.extend(numpy_binaries)
          a.hiddenimports.extend(numpy_hiddenimports)
          "@
          $spec = $spec -replace '(?<=a = Analysis\([\s\S]*?\))', "`n`n$insert"
          # 保持英文名，避免编码问题
          Set-Content -Path 'MonitorApp.spec' -Value $spec

      - name: Build EXE
        run: |
          pyinstaller MonitorApp.spec

      - name: Rename EXE to Chinese name
        shell: powershell
        run: |
          if (Test-Path "dist\MonitorApp.exe") {
              Rename-Item -Path "dist\MonitorApp.exe" -NewName "屏幕数值监控报警.exe"
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 屏幕数值监控报警
          path: dist/*.exe
          retention-days: 30
