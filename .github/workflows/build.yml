name: 屏幕数值监控报警

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip' # 💡 新增：开启 Pip 依赖缓存，大幅加快后续自动化构建速度

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build with PyInstaller
      run: |
        pyinstaller -D -w -n "屏幕数值监控报警" --icon=favicon.ico --add-data "favicon.ico;." --collect-all paddleocr --collect-all paddle main.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: 屏幕数值监控报警
        path: dist/屏幕数值监控报警/
